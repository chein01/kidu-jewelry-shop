
services:
  db:
    container_name: kidudb
    image: mariadb
    restart: always
    ports:
      - 3307:3306
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_DATABASE: kidustore
      MARIADB_USER: admin
      MARIADB_PASSWORD: admin
    volumes:
      - mariadb_data:/var/lib/mysql


  python:
    container_name: kidubackend
    build:
      context: .
    volumes:
      - .:/app/code
    command: python3 /app/code/manage.py runserver 0:8000
    ports:
      - "8000:8000"
    depends_on:
      - db
  

volumes:
  mariadb_data:

